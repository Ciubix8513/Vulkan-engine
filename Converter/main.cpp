#include "ModelLoader.h"


int main (int argc , char* args[])
{
	if (argc != 2)
		throw std::runtime_error("Wrong arguments");
	//auto m = a::ModelLoader::ConvertOBJ(args[0]);
	//a::ModelLoader::SaveModel(m, args[1]);
	
	return 0; 
}